module Base
{
    item PrayerCards
    {
        DisplayCategory = Deck, Weight = 0.01, Type = Normal,
        DisplayName = Prayer Deck, Icon = ZenerCards/deck, WorldStaticModel = PrayerCards,
    }

}